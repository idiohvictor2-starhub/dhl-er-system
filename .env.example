# Copy to .env and fill in for local dev — never commit the real .env

# Database
PGHOST=localhost
PGPORT=5432
PGUSER=er_dev
PGPASSWORD=er_dev
PGDATABASE=er_case_system
DATABASE_URL=postgresql://er_dev:er_dev@localhost:5432/er_case_system

# API
PORT=4000
JWT_SECRET=change_this_in_every_real_environment
NODE_ENV=development

# Frontend
VITE_API_URL=http://localhost:4000
