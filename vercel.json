{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "vite build apps/web || vite build",
  "outputDirectory": "apps/web/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
